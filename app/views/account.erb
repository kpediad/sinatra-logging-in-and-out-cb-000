<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">
  </head>
  <body id="page-top">
    <% if User.is_logged_in? then %>
        <h2><%=User.current_user.username%></h2>
        <br>
        <h2>Balance: <%=User.current_user.balance%></h2>
        <br>
        <a href="/logout">Log Out</a>
    <% else %>
        <a href="/">Home</a>
    <% end %>
  </body>
</html>
